# 작업 원칙

- 사용자가 명령한 범위를 넘어서는 작업을 하지 않는다.
- 추가 작업이 필요하다고 판단되더라도 추측에 따라 실행하지 않는다.
- 필요한 작업이라고 판단되면 먼저 사용자에게 물어보고 확인을 받은 뒤 실행한다.
- UI 작업 전에는 컴포넌트를, 함수 작업 전에는 유틸을 먼저 확인하고 이번 작업에 재사용할 수 있는 것을 재사용한다.
- 미리 작성되어 있는 내용은 사용자가 명시적으로 수정하라고 하지 않는 이상 수정하지 않는다.
- 기존 내용의 수정이 필요하다고 판단되면 먼저 사용자에게 확인을 받고 진행한다.
- 커밋, 푸시, 배포는 사용자가 명시적으로 하라고 요청했을 때만 실행한다.
- 각 디렉터리의 하위 폴더에 `RULE.md`가 있는지 확인하고, 존재하면 해당 내용을 참고한다.
- 프로젝트 파일에는 주석으로 내용을 정리하지 않고, 해당 파일과 같은 이름의 `.md` 파일에 관련 내용을 정리한다.
- 같은 이름의 `.md` 파일이 없으면 생성한다.

## 커밋·푸시 절차

- 커밋과 푸시는 사용자가 명시적으로 요청했을 때만 실행한다.
- 실행 전에 `git status`와 `git diff`로 변경 범위를 확인한다.
- 요청된 파일만 `git add <파일>`로 스테이징한다.
- `git diff --cached`로 커밋 대상 내용을 확인한다.
- 확인된 내용만 `git commit -m "<커밋 메시지>"`로 커밋한다.
- 현재 브랜치와 원격을 확인한 뒤 `git push origin <브랜치>`로 푸시한다.
- 인증 정보가 없거나 명령이 실패하면 추측하거나 우회하지 않고 사용자에게 먼저 알린다.
- 사용자가 명시적으로 요청하지 않는 한 다른 프로젝트의 파일, 코드, 규칙, 구조를 참고하지 않는다.
- 각 `RULE.md`에는 해당 기능을 왜 필요한지에 대한 의도를 함께 적는다.
- 사용자가 기능의 의도를 전달하지 않았으면 필요한 의도를 추측하되, 추측한 내용을 사용자에게 먼저 물어보고 허가받은 뒤 진행한다.
