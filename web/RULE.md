# 웹 규칙

웹에서 발생하는 클릭 이벤트와 웹 라우트 등의 동작은 DB에 기록되어야 합니다.

기록에는 어떤 API 호출이, 언제, 어떻게, 어디에서, 누구로 인해 이루어졌는지가 포함되어야 합니다.

## 서버 통신 오류 처리

- 서버와 통신하는 모든 코드는 `try/catch`로 에러를 처리해야 합니다.
- 통신 중 발생한 모든 에러는 toast로 사용자에게 표시해야 합니다.
