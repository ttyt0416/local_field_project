# 동영상 생성 일회성 선택 상태

콘텐츠 라이브러리에서 선택한 기존 Storage `file_id`를 동영상 생성 화면으로 한 번 전달한다. I2V·FL2V의 frame과 R2V의 reference 이미지·동영상·오디오를 mode별로 보관하며, 생성 화면 진입 시 소비하고 삭제한다. R2V reference는 라이브러리에서 중복 없이 여러 개를 누적할 수 있다. 파일 선택 자체는 업로드나 generation 요청을 발생시키지 않는다.
