# IconOutlinedButton

`IconOutlinedButton`은 기본 outlined 아이콘 버튼, 배경색이 있는 `variant="filled"` 아이콘 버튼, 파괴적 동작을 공용으로 지원한다. 삭제 아이콘은 `variant="destructive"`를 사용해 red 배경, destructive border, 전경색을 적용한다. 이미지 위 favorite 버튼은 `variant="filled"`를 사용해 테두리 없이 배경색을 표시한다.
