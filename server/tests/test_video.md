# MiniMax H3 동영상 계약 테스트

세 모드가 각각 올바른 workflow를 사용하고 `SaveVideo` dynamic combo payload를 구성하는지 확인한다. Eros active checkpoint는 canonical beta5 `MiniMaxH3/10Eros_Max_h3_hybrid_beta5.safetensors`이고 선택 시 6 steps를 사용하는지 검증한다. 선택 LoRA order, GPU allocator crash를 피하기 위한 Morton off 상태, PDD의 matching file·Euler sigma schedule·EasyCache exclusion, persisted continuation steps와 PDD state도 검증한다. video prompt enhancement는 empty overall plus nonempty segment request, `temperature=0.6`, selected-language translation instruction, removed MiniMax expert-role sentence, strict `shots[]` object schema, server assembly, overall fields, server-side shot order non-revalidation을 검증한다. Shot-level exact `N/A` placeholder는 final shot line에서 제외하고 top-level sound fields의 `N/A`는 유지하는 contract도 검증한다. Explicit scene and shot input은 schema count enforcement 없이 prompt contract로 description, order, action을 유지하고, missing physics detail과 character별 expression and action을 요구하는지도 확인한다. mode를 명시한 asset 해석과 기존 Storage asset 재사용도 검증한다. 테스트는 ComfyUI `/prompt`를 호출하지 않는다.
