# 서버 규칙

서버에서 이루어지는 API 호출은 DB에 기록되어야 합니다.

기록에는 어떤 API 호출이, 언제, 어떻게, 어디에서, 누구로 인해 이루어졌는지가 포함되어야 합니다.

## 로그 및 에러 DB

- 웹 로그 관련 DB가 필요합니다.
- 서버 로그 관련 DB가 필요합니다.
- 서버에서 발생하는 모든 에러는 DB에 저장되어야 합니다.
- 에러 관련 기록을 위한 별도 DB가 필요합니다.
